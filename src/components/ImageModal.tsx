interface ImageModalProps {
  isOpen: boolean;
  onClose: () => void;
  imageUrl: string;
  sourceUrl: string;
  description?: string;
}

export default function ImageModal({

}: ImageModalProps) {
  return <></>;
}
